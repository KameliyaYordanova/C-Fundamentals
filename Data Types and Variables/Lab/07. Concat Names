using System;

namespace DataType
{
    class Program
    {
        static void Main(string[] args)
        {
            string fisrtName = Console.ReadLine();
            string lastName = Console.ReadLine();
            string delimiter = Console.ReadLine();

            string result = fisrtName + delimiter + lastName;
            Console.WriteLine(result);
        }
    }
}
